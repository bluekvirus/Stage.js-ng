/**
 * All path are based on the **gulpfile.js** file. [not this configure file]
 *
 * Configure
 * ---------
 * 1. plugins:
 * 	  	settings for the plugins)
 * 2. base: 
 * 		dist, 
 * 		watch path:hooks, 
 * 		js[es6?plain? + vendor], 
 * 		assets, 
 * 		production[minify?gzip?]
 *
 * - merged into config/default.js
 *
 * 
 * @author Tim Lauv
 * @created 2015.02.19
 *
 */
exports.config = {
	//gulp plugin configures
	plugins: {

	},

	output: '../public',

	
};